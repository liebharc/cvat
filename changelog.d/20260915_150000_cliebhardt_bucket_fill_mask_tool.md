### Added

- \[UI\] A bucket fill tool in the mask drawing toolbox: click inside an
  area enclosed by an existing brush/lasso/polygon stroke to fill it in
  one click, similar to a paint-bucket tool. The filled region is grown
  slightly past the enclosing stroke so it blends cleanly with it instead
  of leaving a thin gap.
